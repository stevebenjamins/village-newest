<!DOCTYPE html>
<html>
<head>
	
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<script src="//use.typekit.net/irv8fcc.js"></script>
	<script>try{Typekit.load();}catch(e){}</script>
	<link rel="icon" type="image/png" href="<%= image_path('favicon.png') %>">	
	<script>
	  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
	  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
	  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
	  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
	  ga('create', 'UA-194873-27', 'auto');
	  ga('send', 'pageview');
	 </script>
	
	<title>
		<%= @title if @title %>
		<%= "Village - Toronto News" if !@title %>
	</title>
	<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	<%= csrf_meta_tags %>
	
	<meta property="og:image" content="<%= asset_url("facebook-image.jpg") if @body_class != 'movies-body' %><%= asset_url("facebook-image-movies.jpg") if @body_class == 'movies-body' %>" />
	<meta property="og:title" content="<%= "The best of Toronto news. Hand-picked daily." if @body_class != 'movies-body' %><%= "Every outdoor movie playing in Toronto this summer. From 8 different festivals." if @body_class == 'movies-body' %>" />	

</head>
<body class="<%= @body_class if @body_class %>" data-no-turbolink>

<div class="header">
	<%= link_to "Village - Toronto News", root_url, :id => 'logo' %>
	<div class="sharrre">
  		<div class="twitter" data-url="http://www.itsonvillage.com" data-text="Village"></div>
  		<div class="facebook" data-url="http://www.itsonvillage.com" data-text="Village"></div>
	</div>	
	<ul>
		<% if can? :create, Post %>
			<li><%= link_to "New Post", new_post_url()  %> <span>|</span> </li>
		<% end %>
		<% if user_signed_in? %>
			<li style="display:none;"><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
		<% else %>
			<li style="display:none;"><%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %></li>
		<% end %>
		<li><%= link_to "Outdoor Movies 2015", movies_url() %>
			<li><%= link_to "Weekly Email Brief", brief_url() %>
		<li><a href="mailto:steve@itsonvillage.com" id="contact">Contact</a></li>
	</ul>	
	<a href="javascript:;" id="toggle-mobile-navigation">Menu</a>
	<br class="clear">
</div>

<% if notice %><p class="notice"><%= notice %></p><% end %>
<% if alert %><p class="alert"><%= alert %></p><% end %>

<%= yield %>


</body>
</html>
